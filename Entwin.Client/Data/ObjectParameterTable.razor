@using Entwin.Client.Data

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Key</th>
            <th>Value</th>
            <th>Description</th>
        </tr>
    </thead>
    <tbody>
        @if (Items != null)
        {
            @foreach (var item in Items)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Key</td>
                    <td>@item.TryValue()</td>
                    <td>@item.Description</td>
                </tr>
            }
        }
    </tbody>
</table>

@code {
    [Parameter] public IEnumerable<ObjectParameter>? Items { get; set; }
}
