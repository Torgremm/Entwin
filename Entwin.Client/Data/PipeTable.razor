@using Entwin.Client.Data

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Volume</th>
            <th>Material</th>
            <th>Verticality</th>
        </tr>
    </thead>
    <tbody>
        @if (Items != null)
        {
            @foreach (var item in Items)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Volume</td>
                    <td>@item.Material</td>
                    <td>@item.Verticality</td>
                </tr>
            }
        }
    </tbody>
</table>

@code {
    [Parameter] public IEnumerable<Pipe>? Items { get; set; }
}
